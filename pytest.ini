[pytest]

pythonpath = .

testpaths = tests

python_files = test_*.py

addopts = -v --disable-warnings --tb=short --cov-report=html

markers =
    unit: unit tests
    integration: integration tests
    e2e: end-to-end tests
